use axum::Router;
use axum::routing::{get, post, put,delete};

pub mod model;
pub mod handler;
pub mod domain;
pub mod repo;
mod response;
mod request;

pub fn {{table.remove_prefix_table_name}}_router() -> Router {
    Router::new()
    .route("/{{table.remove_prefix_table_name}}/:id",  get(handler::info))
    .route("/{{table.remove_prefix_table_name}}",  post(handler::page))
    .route("/{{table.remove_prefix_table_name}}",  put(handler::update))
    .route("/{{table.remove_prefix_table_name}}",  post(handler::create))
    .route("/{{table.remove_prefix_table_name}}",  delete(handler::delete))
}